# Control plane acceptance audit

Scope: all 51 sections in [the original synthesis](CONTROL_PLANE_REQUIREMENTS.md).
This is an active gap ledger, initially inspected at `279105f` on 2026-09-10.
Implemented mechanisms below are evidence of progress, **not acceptance of the
whole section**. No section is declared fully accepted by this initial pass.
The original requirements remain authoritative; examples and subrequirements
must be checked again before final acceptance.

| Section | Current evidence | Remaining work or verification |
| --- | --- | --- |
| 1. Runtime correctness | `config/metadata.go`, router metadata/attempt/stream tests; both streaming and non-streaming leases record redacted execution health; Batch 70 adds explicit bounded source health states to runtime snapshots; Batch 92 rejects a `local_model` declaration unless local inference and loopback transport are explicit; Batch 93 aligns local-model declarations with `billing_mode: local`; Batch 94 requires that billing mode explicitly rather than defaulting it to `unknown`; Batch 144 derives browser-authenticated catalog presets as `browser_reverse` with `browser_session` credentials and tests every descriptor-backed browser entry; Batch 154 adds a cross-adapter replay/completion matrix for OpenAI Chat, Anthropic Messages and Gemini, including protocol-specific streaming terminals and credential headers | Real configured-provider replay and live compatibility remain environment-dependent |
| 2. Account/credential separation | `config/accounts.go`, `credential.go`, injected adapter resolution; Batch 63 blocks provider deletion while accounts remain bound; Batch 79 surfaces account-inherited credential state/version in source verification; Batch 99 also reports effective source-owned credential posture on account health without exposing references; Batch 115 filters wizard credential/import actions by the selected descriptor while preserving the compatible OpenAI environment-key path; Batch 128 validates structured username/password writes in the protected store; Batch 139 makes provider changes with bound sources an explicit migration conflict and shared validation guidance; Batch 153 adds reviewed credential unbinding across account/source references | Finish provider-specific credential setup and migration UX |
| 3. Account model | Account configuration plus `/admin/status` account health aggregation: health/auth state, enabled/Auto policy, capacity, sources, counters, last success/failure and last validation; Batch 69 maps login-required and access-denied authentication evidence to `auth_required`; Batch 71 propagates a disabled parent Provider to account health; Batches 97-99 add redacted effective credential state and protected vault version; explicit account and provider validation entry points; Batch 60 blocks deletion while sources remain bound; Batch 131 adds validated account routing defaults and effective-source inheritance | Broader account lifecycle and provider-specific health evidence |
| 4. Protected store | DPAPI; Linux native Secret Service integration tests; AES-GCM envelope tests; credentials package cross-compiles for darwin/amd64 and linux/amd64 with cgo disabled; Batch 96 persists last-use metadata with the next encrypted vault write; Batch 123 adds hosted macOS cgo Keychain execution | Native macOS runner/keychain availability is now checked in CI; an operator's local Keychain remains environment-dependent |
| 5. Add any account | Batch 27 account wizard combines credential/import selection and profile login; Batch 65 adds explicit OpenAI organization/project fields to source configuration and forwards them as scoped headers; Batch 100 filters stored credentials by the selected adapter's declared modes; Batch 115 constrains manual credential types and setup/import actions to provider capabilities, with browser-only actions shown for browser-capable descriptors; Batch 117 renders the descriptor's secret-field label in manual setup; Batch 121 declares username/password as login material for browser-auth-check adapters and verifies the Claude Web wizard import-to-save path; Batch 126 hides irrelevant manual fields and shows descriptor/kind-specific API-key or username/password controls; Batch 130 bounds organization/project metadata before persistence; Batch 131 adds account-level Base URL/Organization/Project defaults with effective-source inheritance and runtime header coverage | Broader provider-specific login checks still require coverage |
| 6. Browser import | Standard profile metadata and selected CDP Cookie import; Batch 107 lets a discovered profile candidate continue into a CDP-bound account setup draft without copying browser data; Batch 129 keeps all eight discovered browser families selectable in the profile schema and setup flow | All named browser import paths and live profile compatibility |
| 7. Manager import | All named CSV formats, Bitwarden JSON, redacted selection; Batch 121 verifies a password-manager CSV imported from the Claude Web account wizard is bound to the saved account through a protected reference; Batch 128 keeps imported username/password material aligned with the store's structured-value validator | Verify all named format fixtures through the account wizard |
| 8. Provider matching | Exact catalog-domain/type suggestions plus official product/API aliases with lookalike rejection; Batch 72 makes account-discovery suggestions host-exact after URL normalization | Broader alias coverage and direct binding workflow |
| 9. Launch login | Dedicated browser launch and bounded UI watcher; Batch 119 drives the Claude Web setup flow through a selected existing profile and verifies launch plus bounded check requests in the browser regression | Broader provider detection and complete account creation flow |
| 10. Profile registry | Profile CRUD, per-account CDP/session isolation; Batch 61 blocks profile deletion while accounts remain bound; Batch 107 configures discovered profile metadata into the existing wizard with an explicit loopback endpoint; Batch 129 validates Chrome, Edge, Brave, Firefox, Opera, Vivaldi, Chromium and Arc engines and uses Firefox's isolated profile launch flag | Existing-profile lifecycle status and live CDP compatibility |
| 11. Full management pages | Provider Detail with explicit provider validation, runtime health/auth, pool strategy, catalog provenance, verified-source count and freshness timestamps; global workload/queue, buffer and Go-memory metrics; provider/account health and capacity rows in Health; Batches 97-98 show account credential state/version in Accounts, Provider Detail and Health; source token/cost observations in Source Detail and Metrics; credential last-use metadata in Credentials (Batch 96 keeps it through the next vault write); owned browser process and session capability views; Batch 109 adds shared session metadata and lifecycle controls for ChatGPT Web, China Web, China Next, Zed Hosted, Amazon Q, and Augment; Batch 113 covers Sessions rendering and Clear confirmation; Batch 119 covers provider-specific Claude Web account-login setup and saved-account health rendering in the isolated browser regression | Full end-to-end page acceptance |
| 12. Groups | Batch 28 adds vision requirement, declared input/output rate ceiling and ordered preferences to configuration/UI/router; Batch 54 records declared usage and cost observations without inventing missing tokens; Batch 62 blocks source deletion while group membership remains; Batch 93 prevents a local billing label from being attached to remote metadata | Full policy acceptance and any provider-specific billing controls still require verification |
| 13. Multiple memberships | Source editor and group membership controls; Batch 118 creates a second fallback group in the browser flow and persists the same ordered sources alongside Auto; Batch 155 verifies shared membership remains independently routable across Auto and ordered fallback groups | Native drag and broader membership conflict coverage remain |
| 14. Source Detail | Batch 29 consolidates configuration/models/capacity/runtime/TTFT/evidence and edit/check actions; Batch 106 adds base URL, organization/project, credential and execution metadata; Batch 134 renders inherited account endpoint metadata and credential binding provenance in Source Detail; explicit three-sample benchmark with cancellation tests | Full live-adapter acceptance and aggregate benchmark persistence remain unverified |
| 15. Routing editor | Batch 28 adds five ordered preferences with explicit rate/latency semantics and dispatch tests; Batch 54 exposes bounded runtime token/cost accounting; Batch 155 covers fallback, select, latency and load-balance dispatch boundaries | Hard per-request budget controls remain outside the declared rate ceiling |
| 16. Drag order | Ordered group source widget; Batch 68 verifies persisted two-source ordering in the browser flow and first/next fallback dispatch in the router; Batch 155 verifies ordered fallback selection and disabled-member advancement in the simulator | Native drag event coverage remains limited in headless regression; physical-browser drag remains |
| 17. Independent switches | Provider/account/source/model enable/Auto gates; Batch 67 adds direct Provider, Account and Source Auto actions with central preview/apply and billing notice; Batch 87 exposes provider/account enabled and Auto policy in Health snapshots; Batch 133 verifies each switch independently reaches the routing simulator with the expected exclusion reason; Batch 140 exercises the Source enabled switch in both directions through browser preview/apply; Batch 141 asserts the disabled exclusion in the browser simulator; Batch 146 exercises the model enabled gate through the bulk editor and restores it | Provider/account/model UI-to-runtime matrix remains to be broadened |
| 18. All frontend configuration | Schema-driven forms and config transactions; Batch 73 rejects sources without a configured model through the shared validator; Batch 116 feeds descriptor credential modes into the source editor's schema-driven credential field | Missing requested settings must first exist in schema/runtime |
| 19. Configuration service | Journal, compile/prepare, atomic runtime publication tests; Batch 150 directly checks stale rollback and unknown-target paths leave preparation, persistence and revision state unchanged | Fault and rollback lifecycle audit across all managed resources |
| 20. Versions/rollback | Version history and Compare/restore; Batch 64 adds browser editing, history review and restore coverage; Batch 138 adds browser and API stale-revision conflict plus refreshed successful restore coverage; Batch 150 adds the underlying service rollback conflict regression | Broader rollback lifecycle checks across managed resources |
| 21. Model management | Native API discovery with bounded pagination and provider metadata (display/owner/created/token limits/methods); batch 35 adds filtered multi-selection and atomic enable/Auto/rating/capability edits; Batch 73 requires every configured source to retain at least one model; Batch 142 verifies discovery metadata is returned before configuration; Batch 151 adds bulk assignment of selected model sources to a group through the same transaction | Reverse-provider discovery and live compatibility |
| 22. Discovery approval | Configure-model action persists discovered/canonical IDs as disabled, unrated and not Auto-approved; routing ignores them until explicit policy edits; Batch 142 applies a discovered model through the admin API and verifies the simulator reports `model_disabled` | Verify richer reverse-provider approval |
| 23. Official sources | Generic drivers; batch 34 adds Ark, legacy Hunyuan, Qianfan v2 and two TokenHub region presets with official references and path/auth tests; Batch 65 preserves OpenAI organization/project routing metadata | Live account/model compatibility, other named provider/custom-profile acceptance and optional service-specific parameters |
| 24. Visible source types | Source-kind badge and provider-kind filter; Batch 147 maps catalog descriptors to stable Official API, Cloud API, Aggregator, Web/Browser reverse, App reverse, CLI/subscription, Local and Research labels in the provider list and filter; Batch 148 applies the same human-readable taxonomy to Sources and Source Detail while schema values remain machine-stable; Batch 149 asserts both Official API and Browser reverse rows in the browser regression; Batch 152 applies the normalized label to Provider Detail as well | Consistent complete official/cloud/browser/app/CLI/local/custom taxonomy |
| 25. Explain exclusions | Router Explain in simulator; batches 29/30 add filtered Source/Provider Detail explanations | Full policy-reason acceptance across configured adapters |
| 26. Simulator | Model/protocol/tools/vision/size without upstream request; Batch 74 adds an opt-in ordered candidate list and selected-target result without acquiring a lease; Batch 92 keeps local-only eligibility tied to explicit local inference metadata; Batch 155 checks selected target and disabled reasons for multiple memberships | Broader policy matrices and final-choice explanations across every strategy remain |
| 27. Health | Runtime outcome, TTFT, declared token counts/cost-known state, last status/timestamps and account health/auth aggregation exist; Batch 59 adds the account health table beside source and shared-domain views; Batch 69 consistently classifies login-required and access-denied evidence as `auth_required`; Batch 70 adds source `disabled`, `blocked`, `cooldown`, `exhausted`, `broken`, `degraded`, `healthy` and `untested` states; Batch 75 adds provider-level health, auth, capacity, membership and counters; Batch 77 adds provider last-generation-validation timestamps from source evidence; Batch 78 adds provider/account last-auth-check timestamps; Batch 80 adds catalog implementation/live provenance and verified-source counts; Batch 81 adds pool strategy and weight metadata; Batch 85 adds provider/account validation-state labels (`not_checked`, `verified`, `failed`, `historical`) beside the latest validation timestamp; Batch 86 keeps the provider validation state visible in the Health table with browser coverage; Batch 89 adds source-level validation timestamp/state to runtime health rows; Batch 102 adds source account and credential provenance columns to the Health table | Surface all source/account fields and verify provider-specific health evidence |
| 28. Test Provider | Provider and source validation use the first configured source/model/protocol and record separate connection/auth/request/stream/completion stages and duration; browser-only providers fall back to account authentication evidence | Provider-specific phase detail and live compatibility still require operator checks |
| 29. Live verification | Batch 25 derives status/counts from model/protocol evidence and source/credential-version bindings; rotation invalidation tested; Batch 79 resolves account-inherited credential references for verification state/version reporting; Batch 83 selects the newest validation by timestamp rather than append order; Batch 85 retains the newest audit entry and marks binding-mismatched evidence `historical` while exposing current `verified`/`failed` state; Batch 91 uses evidence sequence as a deterministic tie-breaker for equal timestamps; Batch 95 preserves that tie-breaker while aggregating provider/account health; Batch 132 hashes effective account endpoint defaults and exposes safe credential binding ownership in source/account health | Real configured-provider checks and complete account/capability evidence still require verification |
| 30. Discovery hub | Batch 27 returns import results to account setup with single-result binding; batch 43 adds bounded metadata-only account candidates from vault, configured environments and optional standard-browser profile scan; Batch 72 applies host-exact provider suggestions to stored credential origins; Batch 107 carries the discovered browser engine and lets candidates prefill the account wizard | End-to-end candidate binding and broader provider matching |
| 31. Shared credential | Account reference shared by multiple sources and quota domain; API integration test binds one protected reference to an account, two sources and an Auto request; Batch 136 verifies the Credentials page lists account-inherited source usage without exposing the secret | Live-provider reuse still requires operator verification |
| 32. Manual binding override | Descriptor matching rejects incompatible kinds by default; Batches 100, 103 and 104 enforce provider credential modes in the wizard and API for account/source bindings, including new credential PUTs; account/source `credential_type_override` is persisted, warned in the editor, covered by binding tests, and Batch 108 exposes the account flag in operational views; Batch 115 carries the same contract into provider-specific manual/import actions and browser setup controls; Batch 116 filters source credential choices by adapter while preserving explicit review of an existing incompatible reference; Batch 117 keeps manual field labeling and mode selection descriptor-driven; Batch 143 checks the Codex wizard exposes OAuth, CLI-session and API-key modes while hiding cookies | Provider-specific contract review and end-to-end UI acceptance for every credential kind |
| 33. Quota domain view | Batch 26 adds domain members/shared counters, retiring leases and atomic all-member limit editing; Batch 57 cascades an account quota-domain move to all bound sources transactionally; Batch 58 applies the same invariant to source edits and source-account moves; Batch 66 adds transactional domain rename with browser coverage | Complete UX/runtime audit still requires verification |
| 34. Account pools | Four router strategies; batch 26 adds pool editor, account switches/capacity/weights and UI persistence tests; Batch 81 includes provider strategy and account weight in Health snapshots; Batch 101 adds enabled counts, live in-flight/limit and per-account weights to the pool overview; Batch 155 verifies least-load selection under a held account lease and stable latency selection | Broader live-provider dispatch acceptance remains |
| 35. Sessions | ChatGPT metadata and expire/clear plus capability rows for every source; Batch 109 adds hashed explicit-session metadata for China Web, China Next, Zed Hosted, Amazon Q, and Augment; Batch 111 rotates the Zed provider thread on expiry; Batch 112 verifies shared-client dispatch preserves the explicit unsupported state; Batch 113 adds browser coverage for metadata rendering and Clear confirmation; Batch 120 routes expire/clear through the shared adapter session-manager contract and verifies a Zed Hosted clear mutation through the admin API | Other stateful adapter inventories and controls; browser acceptance |
| 36. Browsers | Batch 37 retains launch handles/identities, lists owned processes, opens provider origins and confirms exact-launch stops; Batch 61 protects bound browser profiles from deletion; synthetic process/UI tests; Batch 124 adds CDP connection rows for page count, configured session limit and bound account/source counts; Batch 129 adds all discovered browser-family launch selectors and Firefox profile arguments | Browser child-process lifecycle across platforms and live provider/session authentication remain environment-dependent |
| 37. Login wizard | Batch 27 unifies existing/isolated profile, import/manual credential and login-to-save with synthetic browser tests; Batch 107 carries discovered profile metadata into the same setup flow without claiming login; Batch 115 shows profile creation, cookie import and login checks only for browser-capable providers while retaining API-key/token setup for OpenAI; Batch 119 completes the Claude Web selected-profile launch/check-to-save path and verifies redacted account status; Batch 121 covers password-manager import-to-account binding for Claude Web; Batch 129 exposes all eight supported browser families and preserves Firefox's dedicated profile semantics | All installed-browser account discovery, provider-specific setup and real provider compatibility |
| 38. Devices | `POST /admin/device/check` read-only doctor reports ADB/OCR paths, connection, resolution, foreground package, app installation, and evidence-backed login/last-test rows; Devices page and synthetic API/UI tests | Physical-device login and live test evidence still require explicit operator action; no emulator operations |
| 39. Implementation status | `GET /admin/implementation` and System > Implementation join catalog kind/reference, descriptor factory, configured counts, catalog live flag and runtime check counts with separate provenance labels; Batch 80 surfaces catalog implementation/live provenance and current verified-source counts in Health; Batch 82 counts the actual validation evidence kind for runtime verified/failed totals | Live compatibility and broken-state history still require explicit provider checks |
| 40. Descriptor registry | Shared validation/factory/schema contract; Batch 115 consumes declared credential modes and browser capability flags for account setup action filtering; Batch 116 reuses the same modes in schema-driven source credential selection; Batch 117 consumes descriptor secret-field labels in manual credential setup; Batch 126 applies the descriptor field label and selected credential kind to visibility and field semantics | More provider-specific credential/discovery/auth capabilities; eliminate residual setup switches |
| 41. Administration API | Persistent CRUD, config, simulator, source/provider checks, account validation, browser process controls and device doctor; Batch 60 returns a clear conflict for account deletion with bound sources; Batch 61 does the same for browser profiles; Batch 62 does the same for group-bound sources; Batch 63 does the same for provider-bound accounts; Batch 156 adds a revision-checked `/admin/credentials/{id}/unbind` transaction that clears inherited account/source references while retaining the protected value | Broader browser/device lifecycle operations |
| 42. Embedded frontend | Go-embedded static HTML/CSS/JS, no Node runtime; Batch 88 verifies root/asset serving, content types and same-origin CSP from the compiled server; Batch 90 locally cross-builds the gateway for linux/amd64, windows/amd64 and darwin/amd64; Batch 123 publishes hosted three-platform build artifacts | Signed distribution artifacts and publication remain outside this workflow |
| 43. Navigation | All named destinations; batch 32 adds bounded runtime execution events and source/outcome filters in Activity; Batch 54 includes declared usage in status/events; Batch 74 exposes ordered simulator candidates and final selection in Routing | Complete content on every destination and broader event coverage |
| 44. Search | Batch 36 adds explicit model/browser/device matches and tests all eight entity navigation paths; Batch 105 includes credential import source and domain metadata in matching; Batch 137 includes safe credential references and account/source endpoint defaults | Broader naming/localization and large-catalog usability acceptance |
| 45. Preview | Config diff, optimistic revision preview, bounded read-only group eligibility/account/source impact counts before apply, credential replacement impact review, and Batch 135 changed-resource ID summaries for same-count edits; Batch 145 adds provider and browser-profile counts and changed IDs to the same review surface | Complete impact coverage for every sensitive action |
| 46. Central validation | Schema validation plus runtime prepare before apply; Batch 73 rejects empty source model lists with a focused regression | All special actions must retain the same invariants |
| 47. Hot reload | Generation pinning and restart-required fields; Batch 122 verifies the runtime warning in both preview and the post-apply Configuration page while the transaction remains persisted | Full browser confirmation of live vs pending settings for every restart-required section |
| 48. Sensitive confirmation | Config preview, credential-delete confirmation, credential replacement impact review, Batch 67 Auto billing notice for metered provider/account/source; Batch 153 adds explicit unbind impact review before removing account/source references; Batch 156 adds a confirmed, optimistic-revision unbind API that reports affected accounts/sources and retains the vault record | All specified sensitive actions and cost warnings acceptance test |
| 49. Secret-safe logs | Redacted vault/import/session/evidence responses; fixed execution categories; batch 32 stores bounded structured events without raw error/body/credential fields; Batch 54 stores only numeric declared usage/cost fields; generation and model-discovery HTTP errors use stable public categories; Batch 79 reports inherited credential state by reference and version only; Batch 110 normalizes the shared HTTP failure boundary and rejects secret-shaped dynamic messages before JSON encoding; Batch 127 adds a CI-enforced static audit rejecting direct production logger sinks | Adapter-specific error semantics and pre-acquisition rejection logging remain subject to future code review; the project intentionally has no runtime logger sink today |
| 50. Five epics | Work split into committed batches | A–E remain incomplete until their component rows are accepted |
| 51. End-to-end workflow | API integration test covers protected credential → account → two inherited sources → Auto group → successful synthetic local API call without editing JSON; Batch 119 adds a browser-driven Claude Web selected-profile login-to-save journey with bounded synthetic launch/check endpoints and no secret extraction; Batch 121 adds browser-driven password-manager import-to-account binding with protected-reference redaction | Browser-driven full install/import/login journey beyond the covered synthetic paths and live-provider call remain external-environment checks |

Next implementation priorities from this pass: truthful live-verification
status and binding provenance, quota/account pool visibility, the unified
onboarding wizard, owned browser/device controls, and runtime event redaction.
Each subsequent batch should update the affected rows and provide tests rather
than treating this ledger itself as proof of completion.
